import React from 'react'
import ReactDOM from 'react-dom/client'
import LoveLetterSPA from './LoveLetterSPA' 
import './index.css' 

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <LoveLetterSPA />
  </React.StrictMode>
)